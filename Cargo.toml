[package]

name = "pam-sys"
version = "0.3.0"
authors = ["Florian Wilkens <mrfloya_github@outlook.com>"]
description = "FFI wrappers for the Linux Pluggable Authentication Modules (PAM)"
repository = "https://github.com/mrfloya/pam-sys"
documentation = "http://mrfloya.github.io/pam-sys/index.html"
readme = "README.md"
license = "MIT"

build = "build.rs"
links = "pam"

[dependencies]
libc = "0.2.2"

[build-dependencies]
pkg-config = "0.3.5"
gcc        = "0.3.8"
